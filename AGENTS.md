# BeautyPay engineering rules
- App Router + Server Components by default.
- Add `use client` only at interaction boundaries.
- Put new behavior in typed React modules, never in `public/legacy`.
- Preserve Persian RTL/accessibility semantics.
- Prefer AVIF/WebP and explicit image dimensions.
- Run `npm run check` before release.
